bases:
- ../../base
namePrefix: prod-
commonLabels:
  env: prod
patchesStrategicMerge:
- staging-configmap.yaml 
- replica-count.yaml
configMapGenerator:
- name: rsvpconfig-staging
  literals:
    - TEXT1="Welcome to"
    - TEXT2="Production"
